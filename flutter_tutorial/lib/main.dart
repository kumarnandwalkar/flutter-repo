import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:flutter_tutorial/app/app.dart';

void main()
{
  // for displaying on screen we use runapp, runapp is a mtd which takes i/p as a widget
  // named app
  runApp(const MyApp()); // here we exported the materialapp as a flutter widget from refactor menu  cuz the code was becomming too long
}

